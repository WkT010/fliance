apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: nexa
resources:
  - namespace.yaml
  - configmap.yaml
  - api-gateway.yaml
  - matching-engine.yaml
  - wallet-service.yaml
  - hpa.yaml
secretGenerator:
  - name: nexa-secrets
    literals:
      - jwt-secret=nexa-production-secret-change-me
